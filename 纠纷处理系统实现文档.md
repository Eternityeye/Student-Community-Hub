# 纠纷处理系统完整实现文档

## 📋 项目概述

本文档描述了校园互助任务平台中的**纠纷处理系统**和**信誉管理系统**的完整实现。

### 核心功能
- ✅ 用户可在任务完成后发起纠纷
- ✅ 双方可上传证据图片进行举证
- ✅ 管理员可裁定纠纷并判定责任
- ✅ 根据纠纷处理结果扣减信誉分
- ✅ 信誉分低于阈值自动禁止接单
- ✅ 定时任务自动解禁用户

---

## 🗄️ 数据库表设计

### 1. 纠纷案件表 (dispute_case)

```sql
CREATE TABLE dispute_case (
  case_id bigint PRIMARY KEY AUTO_INCREMENT,
  task_id bigint NOT NULL,
  plaintiff_id bigint NOT NULL,          -- 申请人(发起方)
  defendant_id bigint NOT NULL,          -- 被申请人(被告方)
  reason varchar(255) NOT NULL,
  plaintiff_description text,
  defendant_description text,
  plaintiff_evidence_images varchar(2000),
  defendant_evidence_images varchar(2000),
  status char(1) DEFAULT '0',            -- 0待审 1申请人责任 2被申请人责任 3驳回
  handler_id bigint,                     -- 处理人(管理员)
  handle_reason varchar(500),
  handle_time datetime,
  create_by varchar(64),
  create_time datetime DEFAULT CURRENT_TIMESTAMP,
  update_by varchar(64),
  update_time datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  KEY idx_task_id, KEY idx_plaintiff_id, KEY idx_defendant_id, KEY idx_status
);
```

### 2. 用户信誉表 (user_credit)

```sql
CREATE TABLE user_credit (
  credit_id bigint PRIMARY KEY AUTO_INCREMENT,
  user_id bigint NOT NULL UNIQUE,
  credit_score int DEFAULT 100,          -- 信誉分(0-100)
  dispute_count int DEFAULT 0,           -- 纠纷次数
  ban_until datetime,                    -- 禁止接单至
  ban_reason varchar(255),
  last_update datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  KEY idx_ban_until
);
```

### 3. 信誉历史表 (credit_log)

```sql
CREATE TABLE credit_log (
  log_id bigint PRIMARY KEY AUTO_INCREMENT,
  user_id bigint NOT NULL,
  change_type varchar(50) NOT NULL,      -- 'dispute_loss'等
  change_score int NOT NULL,             -- 变动分数
  related_id bigint,                     -- 关联ID
  reason varchar(255),
  create_time datetime DEFAULT CURRENT_TIMESTAMP,
  KEY idx_user_id, KEY idx_create_time
);
```

---

## 🏗️ 前端结构

### 创建的前端文件

| 文件 | 说明 |
|------|------|
| `CreateDisputeDialog.vue` | 发起纠纷对话框组件 |
| `DisputeDetail.vue` | 纠纷详情页面 |
| `DisputeList.vue` | 我的纠纷列表页面 |
| `CreditInfo.vue` | 信誉分显示组件 |
| `userDisputes.vue` | 个人中心纠纷tab页 |
| `dispute.js` | 纠纷相关API |

### 修改的前端文件

- ✅ `TaskCard.vue` - 添加"发起纠纷"按钮
- ✅ `profile/index.vue` - 集成信誉分卡片和纠纷tab
- ✅ `router/index.js` - 添加纠纷路由

### 前端路由配置

```javascript
{
  path: '/dispute',
  component: Layout,
  hidden: true,
  children: [
    { path: 'list', component: DisputeList, name: 'DisputeList' },
    { path: ':id', component: DisputeDetail, name: 'DisputeDetail' }
  ]
}
```

---

## ⚙️ 后端实现

### 创建的Java文件

#### Domain (3个)
- `DisputeCase.java` - 纠纷案件实体
- `UserCredit.java` - 用户信誉实体
- `CreditLog.java` - 信誉历史实体

#### Mapper (3个)
- `DisputeCaseMapper.java` - 纠纷案件数据层
- `UserCreditMapper.java` - 用户信誉数据层
- `CreditLogMapper.java` - 信誉历史数据层

#### Service (2个接口 + 2个实现)
- `IDisputeCaseService.java` + `DisputeCaseServiceImpl.java`
- `IUserCreditService.java` + `UserCreditServiceImpl.java`

#### Controller (2个)
- `DisputeController.java` - 纠纷处理API
- `UserCreditController.java` - 信誉查询API

#### 定时任务
- `CreditUnbanTask.java` - 每日凌晨2点自动解禁

---

## 📡 API接口详解

### 1. 纠纷相关接口

#### 1.1 创建纠纷
```
POST /dispute/create
Content-Type: application/json

{
  "taskId": 1,
  "defendantId": 2,
  "reason": "quality_issue",
  "plaintiffDescription": "服务质量不好",
  "plaintiffEvidenceImages": "url1,url2"
}

Response:
{
  "code": 200,
  "msg": "纠纷已提交"
}
```

#### 1.2 查询纠纷列表
```
GET /dispute/list?pageNum=1&pageSize=10&status=0&roleType=plaintiff

Response:
{
  "code": 200,
  "msg": "查询成功",
  "rows": [
    {
      "caseId": 1,
      "taskId": 1,
      "taskTitle": "取快递",
      "plaintiffName": "张三",
      "defendantName": "李四",
      "reason": "quality_issue",
      "status": "0",
      "roleType": "plaintiff",
      "createTime": "2026-03-18 10:30:00"
    }
  ],
  "total": 100
}
```

#### 1.3 查询纠纷详情
```
GET /dispute/{caseId}

Response:
{
  "code": 200,
  "data": {
    "caseId": 1,
    "taskId": 1,
    "plaintiffId": 1,
    "plaintiffName": "张三",
    "plaintiffAvatar": "avatar_url",
    "plaintiffDescription": "服务质量不好",
    "plaintiffEvidenceImages": "url1,url2",
    "defendantId": 2,
    "defendantName": "李四",
    "defendantDescription": null,
    "status": "0",
    "roleType": "plaintiff",
    "createTime": "2026-03-18 10:30:00"
  }
}
```

#### 1.4 提交答辩证据
```
POST /dispute/submitDefense

{
  "caseId": 1,
  "defendantDescription": "我已按时完成了任务",
  "defendantEvidenceImages": "url1,url2"
}

Response:
{
  "code": 200,
  "msg": "答辩已提交"
}
```

#### 1.5 管理员处理纠纷 (仅admin)
```
PUT /dispute/{caseId}/handle

{
  "status": "2",  // 1=申请人责任, 2=被申请人责任, 3=驳回
  "handleReason": "经查证，被申请人未按时完成任务"
}

Response:
{
  "code": 200,
  "msg": "纠纷已处理"
}
```

### 2. 信誉相关接口

#### 2.1 获取用户信誉信息
```
GET /user/credit/{userId}

Response:
{
  "code": 200,
  "data": {
    "creditId": 1,
    "userId": 1,
    "creditScore": 90,
    "disputeCount": 1,
    "banUntil": null,
    "banReason": null
  }
}
```

#### 2.2 获取用户信誉历史
```
GET /user/credit/log/{userId}

Response:
{
  "code": 200,
  "data": [
    {
      "logId": 1,
      "userId": 1,
      "changeType": "dispute_loss",
      "changeScore": -10,
      "relatedId": 1,
      "reason": "纠纷案件1被判定有责任",
      "createTime": "2026-03-18 14:00:00"
    }
  ]
}
```

#### 2.3 检查用户是否被禁用
```
GET /user/credit/{userId}/banned

Response:
{
  "code": 200,
  "data": true  // true表示被禁用
}
```

---

## 🔄 业务流程

### 纠纷处理流程

```
用户发起纠纷
    ↓
检查是否已有纠纷 → 存在则拒绝
    ↓
创建纠纷案件(状态=待审理)
    ↓
被申请人可上传答辩证据
    ↓
管理员审理纠纷
    ↓
管理员做出裁定
    ↓
↓                          ↓
申请人责任 → 扣申请人10分   被申请人责任 → 扣被申请人10分
    ↓
信誉分 ≤ 0 ?
    ↓                      ↓
是 → 禁止接单30天          否 → 结束
    ↓
定时任务检查 (每日凌晨2点)
    ↓
禁止期满 → 自动解禁
```

### 信誉分管理规则

| 操作 | 扣分 | 触发条件 |
|------|------|---------|
| 纠纷被判有责 | 10分 | 纠纷案件状态变为1或2 |
| 初始分数 | - | 100分 |
| 禁止接单 | - | 信誉分 ≤ 0 |
| 禁止期限 | - | 30天 |
| 自动解禁 | - | 定时任务检查 |

---

## 🧪 前端调试检查清单

### 发起纠纷流程
- [ ] 进入任务详情页（状态≥2时）
- [ ] 点击"发起纠纷"按钮
- [ ] 填写纠纷原因和描述
- [ ] 上传证据图片（最多5张）
- [ ] 提交纠纷

### 纠纷详情页
- [ ] 查看申请人信息和证据
- [ ] 如果是被申请人，能上传答辩
- [ ] 查看管理员裁定结果（如已处理）
- [ ] 图片可预览

### 个人信息页
- [ ] 左侧显示信誉分卡片
- [ ] 显示禁止接单警告（如被禁）
- [ ] 显示信誉变动历史
- [ ] "我的纠纷"tab可查看所有相关纠纷

### 响应式设计
- [ ] 桌面端正常显示
- [ ] 平板端布局合理
- [ ] 手机端可正常使用

---

## 🚀 部署步骤

### 1. 数据库初始化
```bash
# 执行SQL文件
mysql -u root -p < sql/dispute.sql
```

### 2. 检查配置

确保以下配置存在：

**application.yml**
```yaml
spring:
  scheduling:
    enable-annotation-processing: true  # 启用定时任务
```

### 3. 验证接口

使用Postman或其他工具测试：
```
GET http://localhost:8080/user/credit/1
```

应返回用户信誉信息。

### 4. 前端配置

确保前端API请求地址正确：
```javascript
// src/utils/request.js
const service = axios.create({
  baseURL: process.env.VUE_APP_BASE_API, // /dev-api
})
```

---

## ⚠️ 常见问题

### Q: 如何禁止接单的用户恢复？
**A:** 有两种方式：
1. 自动：定时任务每天凌晨2点检查，禁止期满自动解禁
2. 手动：可以添加管理员端手动解禁功能

### Q: 同一任务可以有多个纠纷吗？
**A:** 不可以。系统会检查是否已存在纠纷，存在则拒绝创建。

### Q: 信誉分可以恢复吗？
**A:** 目前系统只支持自动扣分，不支持自动恢复分数。可以在后期扩展"申诉"功能。

### Q: 定时任务不执行怎么办？
**A:**
1. 检查`CreditUnbanTask.java`是否在service包下
2. 检查`application.yml`中是否启用了`@Scheduled`
3. 检查服务器时间是否正确

---

## 📝 后续扩展建议

1. **申诉机制** - 用户对裁定不服可申诉，由其他管理员重新审理
2. **信誉恢复** - 添加"良好行为"日期满后自动恢复部分分数
3. **黑名单** - 信誉分极低的用户加入黑名单，彻底禁用账户
4. **举报奖励** - 发起纠纷且获胜可获得分数奖励
5. **管理后台** - 完善管理员端纠纷处理界面和统计报表

---

## 📞 技术支持

如有问题，请检查以下日志：
- 后端日志：`logs/ruoyi.log`
- 浏览器控制台：F12 → Console
- 网络请求：F12 → Network

---

**实现时间**: 2026-03-18
**版本**: 1.0
**状态**: ✅ 完整实现
